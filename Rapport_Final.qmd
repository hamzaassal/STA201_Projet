---
title: "Projet STA201 - Analyse multivariée approfondie"
author:
  - "ASSAL"
  - "TRAORE"
date: "2026"
lang: fr-FR

format:
  pdf:
    documentclass: article
    fontsize: 12pt
    pdf-engine: xelatex
    geometry:
      - margin=2cm
    toc: true
    number-sections: true
    colorlinks: true
    include-in-header:
      text: |
        \definecolor{cnamred}{HTML}{C9002B}
        \renewcommand{\maketitle}{%
          \begin{titlepage}
          \thispagestyle{empty}
          \begin{center}
          \vspace*{1.4cm}
          \makebox[\textwidth][c]{\includegraphics[width=0.42\textwidth]{assets/logo-cnam.png}}
          \vspace{0.8cm}
          {\Large Conservatoire National des Arts et Métiers\par}
          \vfill
          {\Huge\bfseries{} Projet STA201 : Analyse multivariée approfondie \par}
          \vspace{0.45cm}
          {\LARGE{} Etude de cas : Annulations de reservations hotelières \par}
          \vfill
          \begin{tabular}{rl}
          Auteurs : & ASSAL HAMZA \\
                    & TRAORE SAH\\
          Date : & 17-05-2026 \\
          \end{tabular}
          \vspace*{1.4cm}
          \end{center}
          \end{titlepage}
        }
        \makeatletter
        \let\oldtableofcontents\tableofcontents
        \renewcommand{\tableofcontents}{%
          \oldtableofcontents
          \thispagestyle{empty}
        }
        \makeatother

# format:
#   html:
#     toc: true
#     number-sections: true    

execute:
  echo: false
  warning: false
  message: false
---

```{r}
#| label: setup
#| include: false
source("scripts/00-packages.R")
source("scripts/01-import.R")
source("scripts/02-data_engenering.R")
```

\newpage

# Présentation des bases théoriques des deux méthodes

## Régression logistique

La régression logistique est une méthode de classification supervisée utilisée lorsque la variable à expliquer est qualitative, le plus souvent binaire. Elle permet d'estimer la probabilité qu'un individu appartienne à une modalité d'intérêt à partir d'un ensemble de variables explicatives quantitatives ou qualitatives. Lorsque les variables explicatives sont qualitatives, elles sont généralement transformées en variables indicatrices afin d'être intégrées au modèle.

Contrairement à une régression linéaire classique, la régression logistique ne modélise pas directement la variable binaire. Elle modélise le logarithme du rapport des chances, appelé logit. Si l'on note $p_i$ la probabilité que l'individu $i$ appartienne à la classe étudiée, le modèle s'écrit :

$$
\log\left(\frac{p_i}{1-p_i}\right)
=
\beta_0 + \beta_1 x_{i1} + \cdots + \beta_p x_{ip}
$$

où $x_{i1}, \ldots, x_{ip}$ représentent les variables explicatives, et $\beta_0, \ldots, \beta_p$ les paramètres du modèle. La transformation logit permet de relier une probabilité comprise entre 0 et 1 à une combinaison linéaire de variables explicatives.

Une fois le modèle estimé, la probabilité prédite s'obtient par la fonction logistique :

$$
p_i =
\frac{1}{1 + \exp[-(\beta_0 + \beta_1 x_{i1} + \cdots + \beta_p x_{ip})]}
$$

La décision de classer un individu dans la classe étudiée dépend ensuite d'un seuil de classification. Le seuil de 0,5 est souvent utilisé par défaut, mais il peut être modifié selon l'objectif de l'étude. Lorsque l'on cherche prioritairement à détecter une classe particulière, il est fréquent d'ajuster ce seuil afin d'améliorer la sensibilité, c'est-à-dire la capacité du modèle à repérer correctement les individus appartenant réellement à cette classe.

L'interprétation des coefficients se fait généralement à l'aide des odds ratios. Pour une variable explicative donnée, l'odds ratio correspond à l'exponentielle du coefficient estimé :

$$
OR_j = \exp(\beta_j)
$$

Un odds ratio supérieur à 1 indique que la variable augmente les chances d'appartenir à la classe étudiée, toutes choses égales par ailleurs. À l'inverse, un odds ratio inférieur à 1 indique une diminution de ces chances. Cette propriété rend la régression logistique intéressante à la fois pour la prédiction et pour l'interprétation des facteurs associés à une modalité donnée.

L'évaluation du modèle repose sur plusieurs indicateurs issus de la matrice de confusion. L'accuracy mesure la proportion globale de bonnes classifications, mais elle peut être insuffisante lorsque les classes sont déséquilibrées ou lorsque l'on s'intéresse davantage à une classe particulière. La sensibilité, ou recall, mesure la proportion d'individus de la classe d'intérêt correctement détectés. La spécificité mesure la proportion d'individus de l'autre classe correctement reconnus. La précision indique, parmi les individus prédits dans la classe d'intérêt, la proportion qui appartient réellement à cette classe. Le F1-score combine précision et sensibilité, tandis que l'AUC mesure la capacité globale du modèle à séparer les classes indépendamment d'un seuil particulier.

La régression logistique présente plusieurs avantages : elle est relativement simple à mettre en œuvre, interprétable, adaptée à une variable cible binaire et compatible avec des variables explicatives de nature variée. Ses limites tiennent notamment à l'hypothèse d'une relation linéaire entre les variables explicatives et le logit de la probabilité, à la sensibilité aux valeurs extrêmes, à la multicolinéarité et aux situations de séparation quasi parfaite.

## Analyse discriminante sur variables qualitatives

L'analyse discriminante sur variables qualitatives est une approche de classification supervisée adaptée aux situations où les variables explicatives sont principalement qualitatives. Dans une perspective prédictive, elle vise à apprendre une règle permettant d'affecter un nouvel individu à l'un des groupes connus à partir de son profil de modalités. Dans la littérature, cette démarche est souvent désignée par le terme DISQUAL, notamment dans les travaux de Gilbert Saporta.

L'idée générale est de transformer l'information qualitative en une représentation numérique exploitable par une analyse discriminante. Pour cela, les variables qualitatives sont généralement codées sous forme d'un tableau disjonctif complet. Chaque modalité devient une indicatrice prenant la valeur 1 si l'individu possède la modalité, et 0 sinon. Cette transformation permet de représenter chaque individu par le profil des modalités qu'il possède.

À partir de cette représentation, une difficulté apparaît : le tableau disjonctif peut contenir beaucoup de colonnes, parfois corrélées entre elles, surtout lorsque les variables qualitatives ont de nombreuses modalités. Pour rendre la modélisation plus stable, on peut d'abord appliquer une analyse des correspondances multiples (ACM). L'ACM résume l'information qualitative dans un nombre réduit d'axes factoriels continus. Ces axes deviennent alors de nouvelles variables synthétiques qui conservent une partie importante de l'information initiale tout en réduisant la dimension du problème.

La partie discriminante peut ensuite être réalisée à partir de ces axes d'ACM. Deux méthodes classiques peuvent être utilisées : l'analyse discriminante linéaire (LDA) et l'analyse discriminante quadratique (QDA). La LDA suppose que les groupes ont des matrices de variance-covariance identiques ; elle construit donc des frontières de décision linéaires. La QDA relâche cette hypothèse et autorise une matrice de variance-covariance différente pour chaque groupe ; elle peut ainsi construire des frontières de décision plus souples, mais elle demande davantage de données pour être estimée de manière stable.

Dans cette logique, l'analyse discriminante sur variables qualitatives peut être vue comme une chaîne prédictive en deux temps. L'ACM transforme et résume les variables qualitatives, puis la LDA ou la QDA apprend une règle de classification à partir des axes factoriels retenus. La qualité du modèle dépend alors du nombre d'axes conservés, de la séparation réelle entre les groupes et de la stabilité des matrices de covariance estimées.

L'évaluation de la méthode se fait comme pour toute méthode de prédiction supervisée. On applique le modèle à un échantillon de test ou de validation, puis on compare les groupes prédits aux groupes observés. La matrice de confusion permet de calculer l'accuracy, la sensibilité, la spécificité, la précision et le F1-score. Ces indicateurs mesurent la capacité de la méthode à classer correctement les individus et à repérer les groupes d'intérêt.

Cette approche est particulièrement utile lorsque l'analyse porte sur des profils construits à partir de modalités qualitatives. L'étape d'ACM permet aussi de garder une lecture descriptive des modalités qui structurent les axes factoriels, même si l'objectif final est prédictif. Ses limites tiennent notamment à la qualité du codage, au choix du nombre d'axes retenus, à la présence de modalités rares et au risque d'instabilité lorsque certains groupes sont peu représentés. Un regroupement raisonné des modalités peut donc être nécessaire avant l'analyse.

\newpage

# Introduction, problématique et base de données

Notre étude s’appuie sur la base de données *Hotel Booking Demand Datasets*, construite afin d’analyser le comportement d’annulation des réservations dans le secteur hôtelier. Cette base a notamment été diffusée sur Kaggle et documentée dans un article scientifique publié dans la revue *Data in Brief*. Les informations détaillées sur l'origine des données sont précisées dans la section @sec-sources.

La base regroupe les informations de réservation de deux établissements hôteliers situés au Portugal : un hôtel de type *Resort Hotel* et un hôtel de type *City Hotel*. Les deux sous-bases présentent une structure identique composée de 31 variables. Elles contiennent respectivement 40 060 réservations pour le *Resort Hotel* et 79 330 réservations pour le *City Hotel*.

Chaque observation correspond à une réservation hôtelière, qu’elle ait donné lieu à un séjour effectif ou à une annulation. La variable d’intérêt de notre étude est donc la variable indicatrice d’annulation, notée `is_canceled`, qui prend la valeur 1 lorsque la réservation a été annulée et 0 sinon.

L’objectif de cette étude est d’identifier les profils de clients présentant un risque élevé d’annulation. Une meilleure compréhension de ces profils permettrait au management hôtelier de mettre en place des actions ciblées, telles que des relances, une adaptation des conditions de réservation ou une stratégie de surbooking maîtrisée.

La base de données ne contient pas de valeurs manquantes. Toutefois, certaines variables catégorielles, peuvent prendre la valeur « NULL ». Cette valeur ne correspond pas à une donnée absente, mais indique simplement que l’information n’est pas applicable. Par exemple, une valeur « NULL » pour la variable Agent signifie que la réservation n’a pas été effectuée via une agence de voyage et que l'observation n'est pas concernée.

## Présentation des variables importantes à l'étude

Les principales variables utilisées dans l'étude sont décrites en Annexe, dans le Tableau @tbl-dico. Elles couvrent la nature de l'hôtel, la variable cible `is_canceled`, les caractéristiques temporelles de la réservation, la composition du séjour, le canal de réservation, le prix moyen journalier et les demandes particulières du client.

\newpage

# Description des données, prétraitements et statistiques descriptives

La structure générale de la base est résumée en Annexe dans le Tableau @tbl-base-recap. Les variables construites pour l'analyse sont détaillées dans le Tableau @tbl-variables-creees. Les variables retenues pour la modélisation logistique sont présentées dans le Tableau @tbl-variables-modelisation. Ces tableaux sont placés en annexe afin de conserver un corps de rapport plus lisible, conformément à la consigne de ne pas intégrer de listing brut dans le texte principal.

## Nettoyage et cohérence des données

Avant l'analyse statistique et la modélisation, plusieurs contrôles ont été réalisés afin de sécuriser l'utilisation de la base. L'objectif était de conserver l'information disponible tout en traitant les situations susceptibles de produire des incohérences dans les variables construites ou dans les modèles.

La base contient très peu de valeurs manquantes au sens strict de R. Les seules valeurs manquantes identifiées concernent la variable `children`, avec 4 observations. Pour construire la variable `has_children`, ces valeurs ont été assimilées à 0 enfant. Ce choix permet d'éviter la création de valeurs manquantes dans une variable synthétique utilisée ensuite dans les analyses.

Certaines variables de la base utilisent également la modalité `"NULL"`. Dans ce projet, cette modalité n'est pas systématiquement considérée comme une valeur manquante : elle peut traduire une information non applicable. Par exemple, une valeur `"NULL"` pour une agence ou une entreprise peut simplement indiquer que la réservation n'a pas été effectuée par ce canal.

Un contrôle de cohérence a aussi été effectué sur la variable `country`, utilisée pour construire les variables géographiques. Les codes pays non reconnus automatiquement par le package `countrycode` ont été traités explicitement. Les codes `CN`, `ATF` et `UMI` ont été associés à leur continent, tandis que la modalité `"NULL"` a été classée dans la catégorie `Unknown`.

Les observations présentant des incohérences fortes ont ensuite été exclues de la base d'analyse. Il s'agit des réservations sans aucun client renseigné, des séjours de 0 nuit, des valeurs négatives de prix moyen journalier (`adr`) et d'une valeur extrêmement élevée de `adr`. Les doublons exacts ont été repérés mais n'ont pas été supprimés automatiquement, car la base ne contient pas d'identifiant unique de réservation permettant de distinguer avec certitude un doublon technique d'une réservation distincte mais identique sur les variables disponibles.

Enfin, les variables qualitatives utiles à l'analyse ont été converties en facteurs afin de faciliter les statistiques descriptives, l'analyse discriminante sur variables qualitatives et la modélisation logistique.

La synthèse chiffrée de ces contrôles et décisions est présentée en Annexe dans le Tableau @tbl-cleaning-recap.

À l'issue de ces traitements, la base d'analyse contient `r nrow(df_cleaned)` observations. Les variables construites dans la suite du rapport sont donc calculées à partir de cette base d'analyse.

## Préparation des données

La préparation des données consiste ensuite à transformer certaines variables initiales en indicateurs plus directement exploitables pour l'analyse. Plusieurs variables synthétiques ont été construites afin de rendre les résultats plus lisibles, de limiter le nombre de modalités et de disposer de profils interprétables.

Ces variables portent notamment sur le délai de réservation, la durée du séjour, le niveau de prix, la présence d'enfants, l'engagement du client, l'historique d'annulation, la saison touristique et l'origine géographique du client. La variable cible `is_canceled` a également été recodée sous forme factorielle avec les modalités `Non` et `Oui`, ce qui permet une lecture homogène des sorties descriptives et prédictives.

## Analyse statistique univariée

Notre variable d'intérêt étant "is_canceled" sa distribution se présente ainsi:

```{r}
source("scripts/02-data_engenering.R")
source(("scripts/03-stat_desc.R"))

p_target <- df_desc |>
  count(is_canceled) |>
  mutate(
    prop = n / sum(n),
    label = scales::percent(prop, accuracy = 0.1)
  ) |>
  ggplot(aes(x = is_canceled, y = n, fill = is_canceled)) +
  geom_col(width = 0.65, alpha = 0.95) +
  geom_text(aes(label = label), vjust = -0.35, fontface = "bold") +
  scale_fill_manual(values = c("Non" = "#1F77B4", "Oui" = "#E76F51")) +
  scale_y_continuous(labels = scales::comma) +
  labs(
    title = "Répartition des réservations annulées et non annulées",
    x = "Annulation",
    y = "Effectif",
    fill = "Annulation"
  ) +
  theme_report

res_lead <- list(
  density = df_engineered |>
    drop_na(lead_time, is_canceled) |>
    ggplot(aes(x = lead_time, color = is_canceled, fill = is_canceled)) +
    geom_density(alpha = 0.18, linewidth = 1) +
    scale_color_manual(values = c("Non" = "#1F77B4", "Oui" = "#E76F51")) +
    scale_fill_manual(values = c("Non" = "#1F77B4", "Oui" = "#E76F51")) +
    labs(
      title = "Distribution du délai de réservation selon l'annulation",
      x = "Lead time",
      y = "Densité",
      color = "Annulation",
      fill = "Annulation"
    ) +
    theme_report
)
```
```{r}
#| label: fig-target
#| fig-cap: "Répartition des annulations de réservations"
p_target
```


L'histogramme @fig-target présente la répartition des réservations annulées et non annulées.

37% des reservations sont annulées, ce qui reste un niveau élévé d'ou l'importance de ce type d'étude et qui nous donne une base relativement équilibré entre les deux modalités.

## Analyse statistique bivariée
Dans la revue de littérature sur des comportements similaires (annulation dans le secteur aéronautique ou autre), quelques variables expliquant ce comportement ont retenu notre attention, notamment parce qu’elles sont présentes dans notre base de données.
Il s’agit du lead time, des demandes spéciales, des clients ayant déjà annulé, ainsi que des longs séjours.

Voici un panorama de leurs statistiques bivariées avec notre variable d’intérêt.


```{r}
#| label: fig-leadtime
#| fig-cap: "Fonction de densité du délai entre la date reservation et l'arrivée"
res_lead$density
```
La comparaison des fonctions de densités @fig-leadtime

\newpage

# Objectif de l'étude et démarche choisie

L'objectif opérationnel du projet est de mieux identifier les réservations susceptibles d'être annulées. La démarche retenue consiste d'abord à construire des variables qualitatives interprétables, puis à mobiliser deux méthodes : la régression logistique, pour estimer une probabilité d'annulation et interpréter l'effet des facteurs explicatifs, et l'analyse discriminante sur variables qualitatives, pour étudier la discrimination entre les profils de réservations annulées et non annulées.

\newpage

# Résultats de la première méthode : régression logistique

Cette partie presente la construction des modeles de regression logistique. L'objectif est double : comparer plusieurs strategies de selection de variables et evaluer l'apport des regularisations ridge, lasso et elastic net. Comme pour l'analyse discriminante, le test final n'est pas utilise dans cette section. Les modeles sont estimes sur train, compares sur validation, puis le meilleur modele est reestime sur train + validation avant l'evaluation finale dans la partie comparaison.

```{r}
#| label: setup-logistique
#| include: false
invisible(capture.output(source("scripts/06-logistic.R")))
```

## Strategie de modelisation

La regression logistique modelise directement la probabilite d'annulation d'une reservation. Les variables explicatives retenues dans la base de modelisation sont celles preparees dans `scripts/04-data preparation.R`, ce qui garantit que les modeles logistiques utilisent exactement les memes observations que l'analyse discriminante.

Deux familles de modeles ont ete comparees. La premiere famille regroupe les regressions logistiques classiques avec differentes methodes de selection : modele complet, selection backward, selection forward et selection stepwise. Ces modeles sont compares notamment par leur AIC et par leurs performances sur validation.

La deuxieme famille regroupe les regressions logistiques penalisees : ridge, lasso et elastic net. Ces methodes sont utiles lorsque les variables indicatrices issues des variables qualitatives sont nombreuses ou correlees. Ridge reduit les coefficients sans les annuler, lasso peut selectionner automatiquement certaines modalites en mettant des coefficients a zero, et elastic net combine ces deux comportements.

```{r}
#| label: tbl-methodologie-logistique
#| tbl-cap: "Synthese de la strategie retenue pour la regression logistique"
knitr::kable(
  logistic_models_recap,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modele retenu",
    "Type",
    "Seuil",
    "Base de selection",
    "Base finale",
    "Role du test"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

## Comparaison des modeles sur validation

Le choix du modele repose d'abord sur la performance predictive observee sur validation. L'AUC mesure la capacite generale du modele a separer les reservations annulees des reservations non annulees, independamment du seuil. Le F1-score complete cette lecture car il tient compte a la fois du rappel et de la precision pour la modalite d'interet, ici l'annulation.

```{r}
#| label: tbl-logit-validation
#| tbl-cap: "Performances des modeles logistiques sur validation"
logistic_validation_report <- logistic_validation_metrics |>
  dplyr::select(
    model,
    model_type,
    threshold,
    accuracy,
    auc,
    sensitivity_recall,
    specificity,
    precision,
    f1_score,
    balanced_accuracy
  )

knitr::kable(
  logistic_validation_report,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modele",
    "Type",
    "Seuil",
    "Accuracy",
    "AUC",
    "Recall",
    "Specificite",
    "Precision",
    "F1-score",
    "Balanced accuracy"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

```{r}
#| label: fig-logit-validation-auc-f1
#| fig-cap: "AUC et F1-score des modeles logistiques sur validation"
logistic_validation_metrics |>
  dplyr::select(model, auc, f1_score) |>
  tidyr::pivot_longer(
    cols = c(auc, f1_score),
    names_to = "metric",
    values_to = "value"
  ) |>
  dplyr::mutate(
    model = forcats::fct_reorder(model, value, .fun = max)
  ) |>
  ggplot(aes(x = value, y = model, color = metric)) +
  geom_point(size = 2.5) +
  facet_wrap(~metric, scales = "free_x") +
  scale_x_continuous(labels = scales::percent_format(accuracy = 1)) +
  labs(
    x = "Valeur sur validation",
    y = "Modele",
    color = "Metrique"
  ) +
  theme_minimal(base_size = 12) +
  theme(legend.position = "bottom")
```

Les resultats montrent que les modeles logistiques classiques et penalises sont proches. Dans notre execution, le modele retenu automatiquement est `r logistic_models_recap$selected_model`, avec un seuil de `r round(logistic_models_recap$selected_threshold, 2)`. Ce choix est effectue sur validation et non sur test.

## AIC et complexite des modeles

L'AIC permet de comparer les modeles logistiques classiques en arbitrant entre qualite d'ajustement et complexite. Pour les modeles penalises, nous calculons un AIC approximatif fonde sur la log-vraisemblance et le nombre de coefficients non nuls. Il ne doit pas etre interprete comme un AIC strictement equivalent a celui d'un `glm`, mais il donne un repere utile pour comparer ridge, lasso et elastic net entre eux.

```{r}
#| label: tbl-logit-aic
#| tbl-cap: "AIC et AIC approximatif des modeles logistiques"
knitr::kable(
  logistic_aic_table,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modele",
    "Type",
    "Alpha",
    "Lambda 1se",
    "Log-vraisemblance train",
    "Coefficients non nuls",
    "AIC / AIC approx."
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

## Choix du seuil de classification

Une regression logistique produit une probabilite d'annulation. Pour transformer cette probabilite en classe predite, il faut choisir un seuil. Le seuil par defaut de 0,50 n'est pas necessairement le plus adapte, car l'objectif metier consiste surtout a bien detecter les annulations sans degrader excessivement la precision.

Le seuil est donc optimise sur validation apres le choix du modele. Le critere principal est le F1-score, qui equilibre precision et rappel.

```{r}
#| label: fig-logit-seuil
#| fig-cap: "Evolution des metriques selon le seuil du modele logistique retenu"
threshold_table |>
  dplyr::select(
    threshold,
    accuracy,
    sensitivity_recall,
    specificity,
    precision,
    f1_score,
    balanced_accuracy
  ) |>
  tidyr::pivot_longer(
    cols = -threshold,
    names_to = "metric",
    values_to = "value"
  ) |>
  ggplot(aes(x = threshold, y = value, color = metric)) +
  geom_line(linewidth = 0.9) +
  geom_vline(
    xintercept = logistic_models_recap$selected_threshold,
    linetype = "dashed",
    color = "black"
  ) +
  scale_y_continuous(labels = scales::percent_format(accuracy = 1)) +
  labs(
    x = "Seuil de classification",
    y = "Valeur",
    color = "Metrique"
  ) +
  theme_minimal(base_size = 12) +
  theme(legend.position = "bottom")
```

```{r}
#| label: tbl-logit-seuil
#| tbl-cap: "Seuil retenu pour le modele logistique"
threshold_best_logit <- threshold$best_f1 |>
  dplyr::select(
    threshold,
    accuracy,
    sensitivity_recall,
    specificity,
    precision,
    f1_score,
    balanced_accuracy
  )

knitr::kable(
  threshold_best_logit,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Seuil",
    "Accuracy",
    "Recall",
    "Specificite",
    "Precision",
    "F1-score",
    "Balanced accuracy"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

## Variables importantes

L'interpretation depend du type de modele retenu. Pour une regression logistique classique, les odds ratios permettent de lire le sens et l'intensite de l'association entre une modalite et la probabilite d'annulation. Un odds ratio superieur a 1 indique une augmentation des chances d'annulation, tandis qu'un odds ratio inferieur a 1 indique une diminution, toutes choses egales par ailleurs.

Pour les modeles penalises, l'importance est calculee a partir de la valeur absolue des coefficients standardises par le coefficient maximal du modele. Le lasso et l'elastic net peuvent aussi indiquer quelles modalites sont effectivement conservees, car certains coefficients peuvent etre mis a zero.

```{r}
#| label: tbl-logit-variables-meilleur-modele
#| tbl-cap: "Variables ou modalites les plus importantes du modele logistique retenu"
if (best_logistic_model_type == "glm_selection") {
  best_logistic_variables_report <- best_logistic_coefficients |>
    dplyr::filter(term != "(Intercept)") |>
    dplyr::transmute(
      modele = model,
      variable = original_variable,
      modalite = term,
      odds_ratio = estimate,
      p_value = p.value,
      importance = importance
    ) |>
    dplyr::slice_max(importance, n = 15)
} else {
  best_logistic_variables_report <- best_logistic_coefficients |>
    dplyr::transmute(
      modele = model,
      variable = original_variable,
      modalite = dummy_variable,
      odds_ratio = exp(coefficient),
      p_value = NA_real_,
      importance = importance
    ) |>
    dplyr::slice_max(importance, n = 15)
}

knitr::kable(
  best_logistic_variables_report,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modele",
    "Variable",
    "Modalite",
    "Odds ratio",
    "p-value",
    "Importance"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

```{r}
#| label: fig-logit-variables-meilleur-modele
#| fig-cap: "Principales variables ou modalites du modele logistique retenu"
best_logistic_variables_report |>
  dplyr::mutate(
    modalite = stringr::str_replace_all(modalite, "_", " "),
    modalite = forcats::fct_reorder(modalite, importance)
  ) |>
  ggplot(aes(x = importance, y = modalite)) +
  geom_col(fill = "#C9002B", width = 0.7) +
  labs(
    x = "Importance relative",
    y = "Modalite"
  ) +
  theme_minimal(base_size = 11)
```

Pour completer l'analyse des regularisations, le tableau suivant presente l'importance agregee des variables dans les modeles penalises. Cette lecture est utile car une meme variable qualitative peut generer plusieurs indicatrices.

```{r}
#| label: tbl-logit-importance-penalisee
#| tbl-cap: "Importance agregee des variables dans les modeles penalises"
penalized_importance_report <- penalized_variable_importance |>
  dplyr::group_by(model) |>
  dplyr::slice_max(importance_relative, n = 5) |>
  dplyr::ungroup()

knitr::kable(
  penalized_importance_report,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modele",
    "Variable",
    "Modalites retenues",
    "Importance somme",
    "Importance max",
    "Importance relative"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

En synthese, la regression logistique fournit une reference predictive solide et interpretable. Elle permet de quantifier les facteurs associes aux annulations, tout en comparant des versions classiques et penalisees. L'evaluation finale de ce meilleur modele logistique sur la base test est presentee dans la section de comparaison finale, afin de conserver une separation nette entre selection de modele et mesure de performance finale.

\newpage

# Résultats de la deuxième méthode : analyse discriminante sur variables qualitatives

Cette partie présente l'application de la méthode DISQUAL à notre problème de prédiction des annulations. L'idée générale consiste à transformer les variables qualitatives en axes factoriels continus à l'aide d'une ACM, puis à utiliser ces axes comme variables explicatives dans des modèles de classification. Nous comparons ici trois classifieurs construits sur les axes ACM : la LDA, la QDA et le KNN.

```{r}
#| label: setup-analyse-discriminante
#| include: false
invisible(capture.output(source("scripts/05-Analyse Discriminante.R")))
```

## Stratégie méthodologique

L'analyse discriminante a été construite dans une logique strictement prédictive. La base a d'abord été séparée en trois échantillons : train, validation et test. La base train sert à apprendre la représentation factorielle et les modèles ; la base validation sert à choisir le nombre d'axes, le type de modèle et le seuil de classification ; la base test reste réservée à l'évaluation finale.

Une attention particulière a été portée au risque de fuite d'information. Pour la phase de sélection, l'ACM est estimée uniquement sur la base train. Les individus de validation et de test sont ensuite projetés comme individus supplémentaires. Ainsi, la structure factorielle utilisée pour sélectionner le modèle ne dépend pas des observations de validation ni de test.

La sélection du modèle se fait en deux temps. Premièrement, on compare plusieurs nombres d'axes ACM et plusieurs modèles à partir de l'AUC sur validation. L'AUC est utilisée à cette étape car elle évalue la capacité de séparation du modèle indépendamment d'un seuil de classification particulier. Deuxièmement, une fois le meilleur couple modèle / nombre d'axes retenu, le seuil de classification est optimisé sur validation, principalement à partir du F1-score afin de mieux équilibrer précision et rappel sur la classe des réservations annulées.

Après cette sélection, le modèle final est réestimé en utilisant train + validation comme échantillon actif pour l'ACM finale. La base test est alors projetée en individu supplémentaire, puis utilisée une seule fois pour mesurer la performance finale. Cette stratégie permet d'utiliser davantage de données pour le modèle final tout en conservant une évaluation honnête sur test.

```{r}
#| label: tbl-methodologie-discriminante
#| tbl-cap: "Synthèse de la stratégie retenue pour l'analyse discriminante"
knitr::kable(
  discriminant_method_recap,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modèle retenu",
    "Axes retenus",
    "k KNN",
    "Seuil",
    "Axes disponibles",
    "Max axes testés",
    "Max k testé",
    "Base de sélection",
    "Base active finale",
    "Rôle du test"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

## Tests statistiques sur les axes de l'ACM

Avant de lancer la grid search, nous verifions les hypotheses statistiques associees aux axes factoriels issus de l'ACM. Ces tests sont realises sur les axes candidats construits a partir de la base d'entrainement, avant le choix final du nombre d'axes. Cette position est importante : les diagnostics portent sur la representation factorielle disponible, puis la selection predictive decide ensuite combien d'axes conserver.

La LDA suppose une normalite multivariee des variables explicatives dans les groupes et une homogeneite des matrices de covariance entre les classes. La QDA relache l'hypothese d'homogeneite des covariances, mais reste plus stable lorsque les axes utilises ne s'eloignent pas trop fortement d'une structure gaussienne. Ces tests ne decident donc pas seuls du modele final : ils servent a evaluer la pertinence statistique des hypotheses, en complement des performances observees sur validation et test.

```{r}
#| label: tbl-shapiro-axes-acm
#| tbl-cap: "Tests de Shapiro-Wilk sur les axes ACM candidats"
knitr::kable(
  shapiro_axes_results,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c("Axe", "Statistique", "p-value", "Conclusion")
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

```{r}
#| label: tbl-normalite-multivariee-acm
#| tbl-cap: "Tests de normalite multivariee sur les axes ACM candidats"
normality_multivariate_table <- dplyr::bind_rows(
  mardia_test_table,
  henze_zirkler_test_table
)

knitr::kable(
  normality_multivariate_table,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

```{r}
#| label: tbl-box-m-acm
#| tbl-cap: "Test de Box M pour l'homogeneite des matrices de covariance"
knitr::kable(
  box_m_test_table,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c("Test", "Statistique", "Degres de liberte", "p-value", "Conclusion")
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

Lorsque les tests de normalite sont rejetes, l'utilisation de la LDA et de la QDA doit etre interpretee avec prudence : ces methodes restent utilisables comme classifieurs, mais leur justification probabiliste stricte est affaiblie. Lorsque le test de Box M rejette l'homogeneite des matrices de covariance, la QDA devient theoriquement plus flexible que la LDA, mais elle peut aussi etre plus instable si le nombre d'axes est trop eleve. C'est pourquoi le choix final du modele repose sur une validation empirique du nombre d'axes, du type de classifieur et du seuil de classification.

## Choix du nombre d'axes et du classifieur

Le nombre d'axes ACM ne doit pas être choisi uniquement à partir de l'inertie factorielle. Dans notre cadre, l'objectif est prédictif : nous cherchons le nombre d'axes qui améliore réellement la capacité à distinguer les réservations annulées des réservations non annulées. Pour cette raison, une grille de recherche est appliquée sur la base validation.

La grille compare plusieurs nombres d'axes pour la LDA, la QDA et le KNN. Pour le KNN, plusieurs valeurs de `k` sont aussi testées. Ce paramètre correspond au nombre de voisins utilisés pour classer une réservation : une petite valeur de `k` donne un modèle plus local et potentiellement plus instable, tandis qu'une grande valeur lisse davantage la décision mais peut perdre des profils spécifiques.

```{r}
#| label: tbl-grid-modeles-discriminants
#| tbl-cap: "Meilleure performance de validation par famille de modèle"
results_validation_summary_report <- results_validation_summary |>
  dplyr::mutate(
    knn_k = dplyr::if_else(is.na(knn_k), NA_real_, knn_k)
  ) |>
  dplyr::select(
    model,
    n_axes,
    knn_k,
    auc,
    accuracy,
    sensitivity_recall,
    specificity,
    precision,
    f1_score,
    balanced_accuracy
  )

knitr::kable(
  results_validation_summary_report,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Modèle",
    "Axes",
    "k",
    "AUC",
    "Accuracy",
    "Recall",
    "Spécificité",
    "Précision",
    "F1-score",
    "Balanced accuracy"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

```{r}
#| label: fig-grid-auc-discriminante
#| fig-cap: "AUC de validation selon le nombre d'axes ACM et le modèle"
plot_discriminant_grid_auc
```

```{r}
#| label: fig-grid-f1-discriminante
#| fig-cap: "F1-score de validation selon le nombre d'axes ACM et le modèle"
plot_discriminant_grid_f1
```

```{r}
#| label: tbl-grid-knn-discriminante
#| tbl-cap: "Synthèse de la recherche du paramètre k pour le KNN"
knitr::kable(
  knn_validation_summary,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "k",
    "Meilleure AUC",
    "Meilleur F1-score",
    "Nombre d'axes associé à la meilleure AUC"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

Au regard des résultats de validation, le modèle retenu est celui qui présente la meilleure AUC, puis le meilleur F1-score en cas de performances proches. Dans notre cas, la sélection retient le modèle `r discriminant_method_recap$selected_model`, avec `r discriminant_method_recap$selected_n_axes` axes ACM. Pour le KNN, la valeur retenue est `k = r discriminant_method_recap$selected_knn_k`.

## Choix du seuil de classification

Les modèles produisent une probabilité ou un score associé à la modalité "Oui" de `is_canceled`. Le seuil par défaut de 0,50 n'est pas forcément optimal, surtout lorsque l'objectif est de détecter les annulations. Nous avons donc optimisé le seuil sur la base validation après avoir choisi le modèle et le nombre d'axes.

Le critère principal retenu est le F1-score. Ce choix est cohérent avec l'objectif métier : il faut repérer un maximum d'annulations tout en évitant de générer trop de fausses alertes. Le seuil retenu est `r round(discriminant_method_recap$selected_threshold, 2)`.

```{r}
#| label: tbl-seuil-discriminant
#| tbl-cap: "Seuil de classification retenu sur validation"
threshold_best_report <- threshold_discriminant$best_f1 |>
  dplyr::select(
    threshold,
    accuracy,
    sensitivity_recall,
    specificity,
    precision,
    f1_score,
    balanced_accuracy
  )

knitr::kable(
  threshold_best_report,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  digits = 4,
  col.names = c(
    "Seuil",
    "Accuracy",
    "Recall",
    "Spécificité",
    "Précision",
    "F1-score",
    "Balanced accuracy"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 7
  )
```

\newpage

# Comparaison des résultats et conclusion

La comparaison finale confrontera les résultats obtenus avec la régression logistique et avec l'analyse discriminante sur variables qualitatives. Elle devra préciser si les deux méthodes identifient des profils similaires de réservations à risque, discuter leurs avantages respectifs et formuler une conclusion opérationnelle pour le management hôtelier.

\newpage

# Sources des données et références {#sec-sources}

La base utilisée provient du jeu de données *Hotel Booking Demand Datasets*, diffusé sur Kaggle et documenté dans l'article scientifique publié dans *Data in Brief* : [Hotel Booking Demand Datasets](https://www.sciencedirect.com/science/article/pii/S2352340918315191). Les références méthodologiques relatives à la régression logistique et à l'analyse discriminante sur variables qualitatives seront complétées dans cette section.

\newpage

# Annexes

## Tableaux complémentaires

```{r}
#| label: tbl-dico
#| tbl-cap: "Description des principales variables de la base de données"
knitr::kable(
  dico_variables,
  format = "latex",
  booktabs = TRUE,
  longtable = TRUE,
  row.names = FALSE,
  col.names = c("Variable", "Description", "Type", "Choix")
) |>
  kableExtra::kable_styling(
    latex_options = c("repeat_header"),
    full_width = FALSE,
    font_size = 6
  ) |>
  kableExtra::column_spec(1, width = "3.2cm") |>
  kableExtra::column_spec(2, width = "7.6cm") |>
  kableExtra::column_spec(3, width = "2.2cm") |>
  kableExtra::column_spec(4, width = "2.7cm")
```

```{r}
#| label: tbl-base-recap
#| tbl-cap: "Structure générale de la base de données"
knitr::kable(
  table_base_recap,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 8
  ) |>
  kableExtra::column_spec(1, width = "3.9cm") |>
  kableExtra::column_spec(2, width = "6.2cm")
```

```{r}
#| label: tbl-variables-creees
#| tbl-cap: "Variables construites pour l'analyse"
knitr::kable(
  table_variables_creees,
  format = "latex",
  booktabs = TRUE,
  longtable = TRUE,
  row.names = FALSE,
  col.names = c(
    "Variable",
    "Variables sources",
    "Règle de construction",
    "Rôle dans l'analyse"
  )
) |>
  kableExtra::kable_styling(
    latex_options = c("repeat_header"),
    full_width = FALSE,
    font_size = 6
  ) |>
  kableExtra::column_spec(1, width = "3.4cm") |>
  kableExtra::column_spec(2, width = "3.8cm") |>
  kableExtra::column_spec(3, width = "5.4cm") |>
  kableExtra::column_spec(4, width = "3.8cm")
```

```{r}
#| label: tbl-variables-modelisation
#| tbl-cap: "Variables retenues pour la modélisation logistique"
knitr::kable(
  table_variables_modelisation,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  col.names = c("Variable", "Rôle dans l'analyse")
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 8
  ) |>
  kableExtra::column_spec(1, width = "4.2cm") |>
  kableExtra::column_spec(2, width = "7.5cm")
```

```{r}
#| label: tbl-cleaning-recap
#| tbl-cap: "Synthèse des contrôles et traitements de cohérence"
knitr::kable(
  table_cleaning_recap,
  format = "latex",
  booktabs = TRUE,
  row.names = FALSE,
  col.names = c("Critère", "Effectif", "Pourcentage", "Décision")
) |>
  kableExtra::kable_styling(
    latex_options = c("hold_position"),
    full_width = FALSE,
    font_size = 8
  ) |>
  kableExtra::column_spec(1, width = "5.0cm") |>
  kableExtra::column_spec(2, width = "2.0cm") |>
  kableExtra::column_spec(3, width = "2.5cm") |>
  kableExtra::column_spec(4, width = "2.0cm")
```


